# Change Detection in Python 3
Change detection algorithm in sonar images using a Markov-based approach.

Each file contains a main which will show an usage example of the functions included in the file.
